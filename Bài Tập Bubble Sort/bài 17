def partial_bubble_sort(a, k):
    n = len(a)
    for i in range(k):
        for j in range(0, n - 1 - i):
            if a[j] > a[j + 1]:

                a[j], a[j + 1] = a[j + 1], a[j]            
    return a
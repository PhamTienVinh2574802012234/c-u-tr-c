def upper_bound(a, x):
    n = len(a)
    left, right = 0, n - 1
    ans = n
    while left <= right:
        mid = left + (right - left) // 2
        if a[mid] > x:
            ans = mid
            right = mid - 1
        else:
            left = mid + 1
    return ans
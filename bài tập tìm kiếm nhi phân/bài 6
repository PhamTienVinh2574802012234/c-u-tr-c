def lower_bound(a, x):
    low = 0
    high = len(a)
    while low < high:
        mid = (low + high) // 2
        if a[mid] >= x:
            high = mid
        else:
            low = mid + 1
    return low